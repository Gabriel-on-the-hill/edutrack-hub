# =============================================================================
# EduTrack Hub - Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------------------------
# Get from Neon Console → Your Project → Connection Details
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL="postgresql://username:password@ep-xxxxx.us-east-2.aws.neon.tech/edutrack?sslmode=require"

# -----------------------------------------------------------------------------
# AUTHENTICATION (Required)
# -----------------------------------------------------------------------------
# Generate a secure random string (minimum 32 characters)
# Run: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters"

# -----------------------------------------------------------------------------
# APPLICATION (Required for Production)
# -----------------------------------------------------------------------------
# Your application URL (used for redirects, emails, cookies)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node environment
NODE_ENV="development"

# -----------------------------------------------------------------------------
# EMAIL - RESEND (Required for Production)
# -----------------------------------------------------------------------------
# Get from https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxxxxxx"

# Sender email (must be from a verified domain in Resend)
EMAIL_FROM="noreply@yourdomain.com"

# -----------------------------------------------------------------------------
# PAYMENTS - STRIPE (Optional)
# -----------------------------------------------------------------------------
# Get from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_test_xxxxxxxxxxxx"
STRIPE_PUBLISHABLE_KEY="pk_test_xxxxxxxxxxxx"

# Get from Stripe Dashboard → Webhooks → Your endpoint
STRIPE_WEBHOOK_SECRET="whsec_xxxxxxxxxxxx"

# -----------------------------------------------------------------------------
# FILE UPLOADS - CLOUDINARY (Optional)
# -----------------------------------------------------------------------------
# Get from https://console.cloudinary.com/settings/c-xxxxx/api-keys
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="123456789012345"
CLOUDINARY_API_SECRET="your-api-secret"

# -----------------------------------------------------------------------------
# ERROR TRACKING - SENTRY (Optional but Recommended)
# -----------------------------------------------------------------------------
# Get from https://sentry.io → Settings → Projects → Your Project → Client Keys
NEXT_PUBLIC_SENTRY_DSN=""

# For source map uploads (optional)
# Get from https://sentry.io → Settings → Account → API → Auth Tokens
SENTRY_AUTH_TOKEN=""
SENTRY_ORG=""
SENTRY_PROJECT=""

# -----------------------------------------------------------------------------
# GOOGLE INTEGRATION (Optional - for Calendar/Meet)
# -----------------------------------------------------------------------------
# Get from Google Cloud Console
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI=""

# -----------------------------------------------------------------------------
# AI CURRICULUM GENERATOR (Required for AI content generation)
# -----------------------------------------------------------------------------
# URL of the deployed Generator service (Python/FastAPI on Railway or Render)
GENERATOR_API_URL="http://localhost:8000"

# Shared API key for service-to-service auth (must match Generator's GENERATOR_API_KEY)
# Generate: openssl rand -hex 32
GENERATOR_API_KEY="your-shared-api-key"

# -----------------------------------------------------------------------------
# SETUP INSTRUCTIONS
# -----------------------------------------------------------------------------
# 1. Create accounts:
#    - Neon (database): https://neon.tech
#    - Resend (email): https://resend.com
#    - Stripe (payments): https://stripe.com (optional)
#    - Cloudinary (uploads): https://cloudinary.com (optional)
#
# 2. Copy this file:
#    cp .env.example .env.local
#
# 3. Fill in your values above
#
# 4. Generate JWT secret:
#    openssl rand -base64 32
#
# 5. Initialize database:
#    npx prisma generate
#    npx prisma db push
#    npm run db:seed
#
# 6. Start development:
#    npm run dev
#
# -----------------------------------------------------------------------------
# PRODUCTION NOTES
# -----------------------------------------------------------------------------
# - Use strong, unique JWT_SECRET in production
# - Set NEXT_PUBLIC_APP_URL to your actual domain
# - Enable Stripe live keys for real payments
# - Verify email domain in Resend
# - Never commit .env.local to version control!
